# 心情日记（Mood Diary）Android App — 交付说明（v1.1.0）

## APK 信息
- **安装包**：`/root/deepseek/output/心情日记-v1.1.0.apk`（约 10.4 MB）
- **包名**：`com.mooddiary.app`　**版本**：1.1.0（versionCode 2）
- **支持系统**：Android 7.0（API 24）及以上，targetSdk 34
- **签名**：自签 keystore（mood-diary.keystore），可直接安装
- 旧版 v1.0.0 保留在同目录（`心情日记-v1.0.apk`）

## 功能清单（v1.1.0 新增"按小时记录"）
1. **按小时记心情（新版核心）**：点日历任意一天 → 弹出该天 **24 小时心情板**（00:00-23:00），每个小时一个槽位：
   - 已记录的槽位显示该小时的情绪颜色 + Emoji
   - 点任意空槽 → 记录那一小时的心情；点已记录的槽 → 修改/删除
   - 一天最多可记 24 条，`(日期, 小时)` 唯一
2. **日历热力图**：格子颜色 = 当天最新一条心情；今天高亮；可切月、回到今天；底部提示"点击某天 → 按小时记录心情"
3. **5 种情绪**：开心 😄 / 平静 😌 / 一般 😐 / 低落 😔 / 生气 😡（1-5 分）
4. **快速记录**：右下角 + 直接记录"现在这个小时"的心情
5. **编辑弹窗**：日期可改（DatePicker）、时间固定为该小时段（如 14:00-14:59）、选情绪、写备注、可删除
6. **记录列表**：按日期+小时倒序（如 `08-30 14:00`），点击可编辑
7. **统计页**：当月记录天数、平均心情分、情绪分布（按天主心情）、本月逐日热力点条、当月总记录条数
8. **Room 持久化**（数据库 v2，旧数据自动迁移清空重建），中文界面，支持系统深/浅色

## 技术栈
Kotlin 1.9.24 · Jetpack Compose（BOM 2024.06.00）· Material 3 · Room 2.6.1 · AGP 8.4.2 · Gradle 8.7
minSdk 24 / compileSdk-targetSdk 34；core library desugaring 支持 java.time

## 构建命令（本项目内）
    JAVA_HOME=/usr/lib/jvm/java-17-openjdk-arm64 /opt/gradle/gradle-8.7/bin/gradle --no-daemon assembleRelease

## 关键工程说明
- ARM64 主机：AGP 默认 aapt2（x86_64）无法运行，gradle.properties 已配置
  `android.aapt2FromMavenOverride=/opt/android-sdk/build-tools/34.0.0/aapt2`
- 签名文件 mood-diary.keystore（storepass/keypass：mooddiary123）
- 数据实体 MoodEntry(date, hour, moodId, note)，唯一索引 (date, hour)
