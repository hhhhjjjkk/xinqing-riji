# 心情日记（Mood Diary）Android App — 交付说明（v1.9.0）

## APK 信息

- **包名**：`com.mooddiary.app`　**版本**：1.9.0（versionCode 13）
- **支持系统**：Android 7.0（API 24）及以上，targetSdk 34
- **签名**：仓库**不再包含**签名密钥；凭据由仓库外的 `keystore.properties` 或环境变量提供
- **构建**：自带 Gradle Wrapper，`./gradlew assembleRelease`

> ⚠️ **v1.4.0 更换了签名密钥**。旧密钥（`mood-diary.keystore`，其密码曾随仓库公开）
> 曾随公开仓库泄漏，已作废并从版本库移除。因此已装 v1.3.0 及更早版本的设备
> **无法原地覆盖升级**，需先卸载再安装。详见 README「关于升级」。

## 功能清单

1. **按小时记心情**：点日历任意一天 → 弹出该天 **24 小时心情板**（00:00-23:00），每个小时一个槽位
   - 已记录的槽位显示该小时的情绪颜色 + Emoji
   - 点任意空槽 → 记录那一小时的心情；点已记录的槽 → 修改/删除
   - 一天最多 24 条，`(日期, 小时)` 唯一
2. **日历热力图**：格子颜色 = 当天最新一条心情；今天高亮；可切月、回到今天
3. **5 种情绪**：开心 😄 / 平静 😌 / 一般 😐 / 低落 😔 / 生气 😡（1-5 分）
4. **快速记录**：右下角 + 直接记录“现在这个小时”的心情
5. **编辑弹窗**：日期可改（DatePicker）、时间固定为该小时段、选情绪、写备注（上限 500 字）、可删除
   - 改日期若撞上已有记录，**会弹窗询问是否覆盖，不会静默删除**
6. **记录列表**：按日期 + 小时倒序，点击可编辑
7. **统计页**：当月记录天数、平均心情分、情绪分布（按天主心情）、逐日热力点条、当月总条数
8. **每小时提醒**：用 AlarmManager 精确闹钟整点触发，关掉 app / 重启手机后仍会提醒；
   **通知内嵌 5 个表情按钮，点一下直接记录当前小时心情，不跳转 app**；
   当前小时已记录或处于免打扰时段则不发通知。设置页可一键加入电池优化白名单
11. **设置页修复**：设置存储改用 callbackFlow + SharedPreferences 监听器，
   开关/选择器现已可靠生效（原版本除删除外均不可用）
12. **测试通知按钮**：设置页可立即弹出一条带 5 个表情的通知
9. **Room 持久化**（数据库 v2），中文界面，支持系统深/浅色
10. **自定义图标**：「日记本 + 铅笔 + 爱心」线稿，含自适应图标与传统方形/圆形
11. **设置页**：主题模式、每小时提醒开关、免打扰时段、默认心情、每周起始日、清空数据

## 技术栈

Kotlin 1.9.24 · Jetpack Compose（BOM 2024.06.00）· Material 3 · Room 2.6.1 ·
WorkManager 2.9.1 · AGP 8.4.2 · Gradle 8.7
minSdk 24 / compileSdk-targetSdk 34；core library desugaring 支持 java.time

## 构建命令

```bash
export ANDROID_HOME=/path/to/android-sdk
./gradlew assembleRelease
./gradlew test            # 数据层回归测试
```

## 关键工程说明

- **图标**：自适应图标（`mipmap-anydpi-v26/` + 5 档密度前景图）与传统方形/圆形图标
  （API 24/25）；背景层为纯白矢量。重新生成见 `tools/make-icons.md`
- **签名**：凭据不入库。在仓库根目录放 `keystore.properties` 或设置 `MOODDIARY_*` 环境变量；
  缺失时构建仍可成功，但产物未签名
- **隐私**：`allowBackup=false` + `data_extraction_rules.xml`，禁止云备份与设备迁移
- 数据实体 `MoodEntry(date, hour, moodId, note, updatedAt)`，唯一索引 `(date, hour)`
- **数据安全**：`MoodRepository.save()` 在同一事务内做冲突检测；DAO 插入使用
  `OnConflictStrategy.ABORT`。任何路径都不会静默删除已有记录
- **迁移策略**：`fallbackToDestructiveMigrationFrom(1)` 只覆盖无法追溯的 v1；
  v2 之后的升级必须显式提供 Migration，否则构建期即失败
- Room schema 导出到 `app/schemas/`，便于审查与迁移测试
- 回归测试见 `app/src/test/java/com/mooddiary/app/MoodRepositoryTest.kt`
